{
  "name": "voicemail-to-mms",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/diagnostic-wrapper.js && node --check src/index.js && node --check src/core.js",
    "test": "node --test",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "postal-mime": "^2.4.3"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
