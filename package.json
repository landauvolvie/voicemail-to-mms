{
  "name": "voicemail-to-mms",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/diagnostic-wrapper.js && node --check src/index.js && node --check src/core.js && node --check src/audio.js && node --check src/probe.js && node --check src/ogg.js && node --check src/ogg-wasm.js",
    "test": "node --test",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@breezystack/lamejs": "^1.2.7",
    "postal-mime": "^2.4.3",
    "wasm-media-encoders": "^0.7.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
